package sa.sauditourism.employee.constants

object NavConstants {
    const val TAB_ID_HOME = "home"
    const val TAB_ID_SERVICES = "services"
    const val TAB_ID_PEOPLE = "people" //used for routing also, so if changed from firebase it wont work
    const val TAB_ID_ACCOUNT = "account" //used for routing also, so if changed from firebase it wont work
}
